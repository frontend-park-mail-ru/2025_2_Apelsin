import store from "./store"

const pages = {
    'auth': [
        'login',
        'regEmail',
        'regPassword',
        'regUser',
        'regCompany',
        'loginPassword'
    ],
}

export const router = () => {
}